function my_first_function() { // defining function
    var str = "Hello, everybody!"; // defining string
    document.getElementById(button_text).innerHTML = str; // telling HTML what the value of the variable is
}

function my_function() { // defining function
    var sentence = "Grandma ate my brownie" // defining sentence variable
    sentence += " and the entire cake!" // concatenating the string with an operator
    document.getElementById(concatenate).innerHTML = sentence; //telling HTML what's up
}